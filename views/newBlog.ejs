<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>New Blog - Blogify</title>
    <%- include("./parts/header") %>
</head>

<body class="bg-slate-950 text-white font-sans antialiased">
    <%- include("./parts/navbar") %>

        <div class="relative pt-32 pb-20 min-h-screen">
            <div class="container mx-auto px-6 max-w-5xl">
                <div class="text-center mb-12 animate-fade-in">
                    <h1 class="text-4xl md:text-5xl font-bold mb-4 font-display">Share Your <span
                            class="bg-gradient-to-r from-blue-400 to-cyan-400 bg-clip-text text-transparent">Story</span>
                    </h1>
                    <p class="text-slate-400">Create something meaningful and share it with the world.</p>
                </div>

                <div class="glass-card p-8 md:p-12 animate-slide-up">
                    <form action="/Blog/" method="post" enctype="multipart/form-data" class="space-y-8">
                        <!-- Title Input -->
                        <div class="space-y-2">
                            <label class="block text-sm font-medium text-slate-300 uppercase tracking-wide">Blog
                                Title</label>
                            <input type="text" name="title"
                                class="w-full bg-transparent border-b-2 border-slate-700 text-3xl md:text-4xl font-bold py-2 focus:border-blue-500 focus:outline-none transition-colors placeholder-slate-600 font-display"
                                placeholder="Enter an engaging title..." required>
                        </div>

                        <!-- Cover Image Upload -->
                        <div class="space-y-2">
                            <label class="block text-sm font-medium text-slate-300 uppercase tracking-wide">Cover
                                Image</label>
                            <div class="relative border-2 border-dashed border-slate-700 rounded-2xl p-8 text-center hover:border-blue-500 hover:bg-slate-800/30 transition-all cursor-pointer group"
                                id="dropZone">
                                <input type="file" name="coverImage"
                                    class="absolute inset-0 w-full h-full opacity-0 cursor-pointer z-10" id="fileInput"
                                    required>
                                <div class="space-y-2 relative z-0">
                                    <div class="text-4xl group-hover:scale-110 transition-transform duration-300">🖼️
                                    </div>
                                    <p class="text-slate-400 font-medium group-hover:text-blue-400 transition-colors">
                                        Drag & drop or click to upload cover image</p>
                                    <p class="text-xs text-slate-500">Supports JPG, PNG, WEBP (Max 5MB)</p>
                                </div>
                                <div id="previewContainer" class="hidden mt-4 relative rounded-lg overflow-hidden h-64">
                                    <img id="imagePreview" src="" class="w-full h-full object-cover" alt="Preview">
                                    <button type="button" id="removeImage"
                                        class="absolute top-2 right-2 bg-red-500/80 hover:bg-red-600 text-white p-2 rounded-full backdrop-blur-sm transition-colors">
                                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                d="M6 18L18 6M6 6l12 12"></path>
                                        </svg>
                                    </button>
                                </div>
                            </div>
                        </div>

                        <!-- Content Editor -->
                        <div class="space-y-2">
                            <label
                                class="block text-sm font-medium text-slate-300 uppercase tracking-wide">Content</label>
                            <div class="relative">
                                <textarea class="textarea-field font-sans text-lg leading-relaxed min-h-[400px]"
                                    name="body" placeholder="Start writing your amazing story here..."
                                    required></textarea>
                                <div class="absolute bottom-4 right-4 text-xs text-slate-500 pointer-events-none">
                                    Markdown supported</div>
                            </div>
                        </div>

                        <!-- Action Buttons -->
                        <div class="flex items-center justify-end gap-4 pt-6 border-t border-white/5">
                            <a href="/"
                                class="text-slate-400 hover:text-white px-6 py-3 font-medium transition-colors">Cancel</a>
                            <button class="btn-gradient shadow-lg shadow-blue-900/20 px-8 py-3 flex items-center gap-2"
                                type="submit">
                                <span>🚀</span> Publish Blog
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <script>
            // Image Upload Preview Logic
            const fileInput = document.getElementById('fileInput');
            const previewContainer = document.getElementById('previewContainer');
            const imagePreview = document.getElementById('imagePreview');
            const removeImage = document.getElementById('removeImage');
            const dropZone = document.getElementById('dropZone');

            fileInput.addEventListener('change', function (e) {
                const file = this.files[0];
                if (file) {
                    const reader = new FileReader();
                    reader.onload = function (e) {
                        imagePreview.src = e.target.result;
                        previewContainer.classList.remove('hidden');
                        dropZone.querySelector('.space-y-2').classList.add('hidden');
                    }
                    reader.readAsDataURL(file);
                }
            });

            removeImage.addEventListener('click', function (e) {
                e.preventDefault();
                fileInput.value = '';
                previewContainer.classList.add('hidden');
                dropZone.querySelector('.space-y-2').classList.remove('hidden');
            });
        </script>
        <%- include("./parts/script") %>
</body>

</html>