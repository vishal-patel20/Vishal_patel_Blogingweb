<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Sign Up - Blogify</title>
    <%- include("./parts/header") %>
</head>

<body class="bg-slate-950 text-white font-sans antialiased">
    <%- include("./parts/navbar") %>

        <div class="min-h-screen flex pt-20 justify-center items-center">
            <!-- Form Section -->
            <div class="w-full max-w-md p-6 relative z-10">
                <div class="glass-card p-8 md:p-10">

                    <% if (locals.error) { %>
                        <div
                            class="mb-6 p-4 rounded-lg bg-red-500/10 border border-red-500/20 text-red-200 text-sm text-center">
                            <%= error %>
                        </div>
                        <% } %>

                            <div class="mb-8 text-center">
                                <h2
                                    class="text-3xl font-bold mb-2 font-display bg-gradient-to-r from-pink-400 to-purple-400 bg-clip-text text-transparent">
                                    Create Account</h2>
                                <p class="text-slate-400">Join our community today.</p>
                            </div>

                            <form action="/user/signup" method="post" class="space-y-5">
                                <div>
                                    <label for="fullname" class="block text-sm font-medium text-slate-300 mb-2">Full
                                        Name</label>
                                    <input type="text" class="input-field" id="fullname" name="fullname"
                                        placeholder="John Doe" required>
                                </div>

                                <div>
                                    <label for="email" class="block text-sm font-medium text-slate-300 mb-2">Email
                                        address</label>
                                    <input type="email" class="input-field" id="email" name="email"
                                        placeholder="hello@example.com" required>
                                </div>

                                <div>
                                    <label for="Password"
                                        class="block text-sm font-medium text-slate-300 mb-2">Password</label>
                                    <input type="password" class="input-field" id="Password" name="password"
                                        placeholder="••••••••" required>
                                    <p class="mt-2 text-xs text-slate-500">Must be at least 8 characters long.</p>
                                </div>

                                <button type="submit" class="w-full btn-gradient-pink shadow-lg shadow-pink-900/20">
                                    Create Account
                                </button>
                            </form>

                            <div class="mt-8 text-center text-slate-400">
                                Already have an account?
                                <a href="/user/signin"
                                    class="text-purple-400 hover:text-purple-300 font-medium hover:underline">Sign
                                    in</a>
                            </div>
                </div>
            </div>
        </div>

        <%- include("./parts/script") %>
</body>

</html>