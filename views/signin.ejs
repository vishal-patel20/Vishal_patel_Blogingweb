<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Sign In - Blogify</title>
  <%- include("./parts/header") %>
</head>

<body class="bg-slate-950 text-white font-sans antialiased">
  <%- include("./parts/navbar") %>

    <div class="min-h-screen flex pt-20 justify-center items-center">
      <!-- Form Section -->
      <div class="w-full max-w-md p-6 relative z-10">
        <div class="glass-card p-8 md:p-10">
          <div class="mb-10 text-center">
            <h2
              class="text-3xl font-bold mb-2 font-display bg-gradient-to-r from-purple-400 to-blue-400 bg-clip-text text-transparent">
              Welcome Back!</h2>
            <p class="text-slate-400">Please enter your details to sign in.</p>
          </div>

          <form action="/user/signin" method="post" class="space-y-6">
            <div>
              <label for="email" class="block text-sm font-medium text-slate-300 mb-2">Email address</label>
              <input type="email" class="input-field" id="email" name="email" placeholder="hello@example.com" required>
            </div>

            <div>
              <div class="flex items-center justify-between mb-2">
                <label for="Password" class="block text-sm font-medium text-slate-300">Password</label>
                <a href="#" class="text-sm text-purple-400 hover:text-purple-300">Forgot password?</a>
              </div>
              <input type="password" class="input-field" id="Password" name="password" placeholder="••••••••" required>
            </div>

            <button type="submit" class="w-full btn-gradient shadow-lg shadow-purple-900/20">
              Sign In
            </button>
          </form>

          <div class="mt-8 text-center text-slate-400">
            Don't have an account?
            <a href="/user/signup" class="text-purple-400 hover:text-purple-300 font-medium hover:underline">Sign up for
              free</a>
          </div>

          <div class="mt-8 pt-6 border-t border-slate-700/50 text-center text-xs text-slate-500">
            &copy; <%= new Date().getFullYear() %> Blogify. All rights reserved.
          </div>
        </div>
      </div>
    </div>

    <%- include("./parts/script") %>
</body>

</html>